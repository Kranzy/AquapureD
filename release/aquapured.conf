# The log level. [DEBUG_SERIAL, DEBUG, INFO, NOTICE, WARNING, ERROR]
# Pick the highest level, and all levels below will be sent to syslog.
# your syslog settings may be set to only display messages above a certian level
# in which case make sure you use the log_file settings to capture everything
# you want when debugging
# so, NOTICE also prints WARNING & ERROR
#     DEBUG_SERIAL would print everything possible

[AQUACONTROLD]

# 0x50 is Jandy normal SWG, 0xB0 is Zodiac Tri SWG with Chem (Ph,ORP)
SWG_DEVICE_ID = 0x50
#SWG_DEVICE_ID = 0xB0

#LOG_LEVEL=DEBUG_SERIAL
#log_level=DEBUG
#log_level=INFO
log_level=NOTICE

#WEB_DIRECTORY=/var/www/aquapured/
WEB_DIRECTORY=./web/

# Log file (will use syslog if not listed)
LOG_FILE=/var/log/aquapured.log

#socket_port=80
WEB_PORT=8080

# The serial port to access RS485 devices.
SERIAL_PORT=/dev/ttyUSB0

# mqtt stuff
MQTT_ADDRESS = ${MQTT_ADDRESS}
MQTT_USER = ${MQTT_USER}
MQTT_PASSWORD = ${MQTT_PASSWORD}
MQTT_TOPIC = ${MQTT_TOPIC}

# Domoticz settings
# Domoticz Pub topic
#MQTT_DZ_PUB_TOPIC = domoticz/in
# Domoticz Virtual Sensor ID (This is an Alert Virtual Sensor Type) 
#DZIDX_SWG_STATUS_ALERT_SENSOR = 99

[RS485_DEVICES]
[RS485_DEVICE:1]
RS485_TYPE = SWG
RS485_ID = 0x50

#[ONEWIREDEVICES]
#[ONEWIREDEVICE:1]
#DEVICE = 28-011564c516ff
#NAME = Water Temperature
#CACHE_EXPIRE = 5

# Options for the GPIO configuration
#
# GPIO_PIN = That actual GPIO number, not pin number or any other ID. That actual GPIO#.
#
# GPIO_OFF_STATE = 0 or 1  The state the GPIO pin shows when on.
# LOW        0
# HIGH       1
#
# GPIO_PULL_UPDN = 0,1,2 Set internal resistor to Pull up. pull down, off. (probably only needed on home made relay boards)
# PUD_OFF      0
# PUD_DOWN     1
# PUD_UP       2
#
# Uncoment below as required (#-# used for RPi)
#-#[GPIOS]
#-#[GPIO:1]
#-#GPIO_NAME = Pump Speed Low
#-#GPIO_PIN = 5
#GPIO_PULL_UPDN = 1
#-#GPIO_OFF_STATE = 0
#-#[GPIO:2]
#-#GPIO_NAME = Pump Speed Medium
#-#GPIO_PIN = 6
#GPIO_PULL_UPDN = 1
#-#GPIO_OFF_STATE = 0
#-#[GPIO:3]
#-#GPIO_NAME = Pump Speed High
#-#GPIO_PIN = 13
#GPIO_PULL_UPDN = 1
#-#GPIO_OFF_STATE = 0
#-#[GPIO:4]
#-#GPIO_NAME = Pump Speed Boost
#-#GPIO_PIN = 19
#GPIO_PULL_UPDN = 1
#-#GPIO_OFF_STATE = 0
#[GPIO:5]
#GPIO_NAME = Spa Blower
#GPIO_PIN = 26
#GPIO_PULL_UPDN = 1
#GPIO_OFF_STATE = 0
#[GPIO:6]
#GPIO_NAME = Pump Speed Medium
#GPIO_PIN = 16
#GPIO_PULL_UPDN = 1
#GPIO_OFF_STATE = 0
#[GPIO:7]
#GPIO_NAME = Pump Speed High
#GPIO_PIN = 20
#GPIO_PULL_UPDN = 1
#GPIO_OFF_STATE = 0
#[GPIO:8]
#GPIO_NAME = Pump Speed Boost
#GPIO_PIN = 21
#GPIO_PULL_UPDN = 1
#GPIO_OFF_STATE = 0
